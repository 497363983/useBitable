import { useFileReader } from "@qww0302/use-bitable"
import { describe, expect, it } from "vitest"

describe("useFileReader", () => {
  it("should be defined", () => {
    expect(useFileReader).toBeDefined()
  })
})